const HR = {
  employees: [],
  init() { this.setToday(); },
  setToday() { const date = new Date().toISOString().slice(0, 10); ['hrAttendanceDate','hrAdvanceDate'].forEach(id => { const el=document.getElementById(id); if(el && !el.value) el.value=date; }); const month=document.getElementById('hrPayrollMonth'); if(month && !month.value) month.value=date.slice(0,7); },
  money(v) { return App?.formatNumber ? App.formatNumber(v || 0) : Number(v || 0).toLocaleString('en-US'); },
  esc(v) { const d = document.createElement('div'); d.textContent = v ?? ''; return d.innerHTML; },
  async load() {
    await Promise.all([this.loadDashboard(), this.loadEmployees(), this.loadAttendance(), this.loadLeaves(), this.loadAdvances(), this.loadPayroll()]);
  },
  async loadDashboard() {
    const res = await fetch('/api/hr/dashboard'); const json = await res.json(); if (!json.success) return;
    const d=json.data, set=(id,v)=>{const e=document.getElementById(id);if(e)e.textContent=v;};
    set('hrActiveEmployees',d.activeEmployees); set('hrPresentToday',d.presentToday); set('hrUnpaidPayroll',this.money(d.unpaidPayroll)+' ر.ي'); set('hrActiveAdvances',this.money(d.activeAdvances)+' ر.ي'); set('hrPendingLeaves',d.pendingLeaves);
  },
  async loadEmployees() {
    const json=await (await fetch('/api/hr/employees')).json(); if(!json.success)return; this.employees=json.data||[];
    const tbody=document.getElementById('hrEmployeesTableBody'); if(!tbody)return;
    tbody.innerHTML=this.employees.length?this.employees.map(e=>`<tr><td>${this.esc(e.employee_no)}</td><td><b>${this.esc(e.full_name)}</b><br><small>${this.esc(e.phone||'-')}</small></td><td>${this.esc(e.job_title||'-')}</td><td>${this.esc(e.department||'-')}</td><td>${this.esc(e.project_name||'الإدارة العامة')}</td><td>${this.money(e.basic_salary)} ${this.esc(e.currency||'ر.ي')}</td><td><span class="status-badge ${e.status==='active'?'status-active':'status-inactive'}">${e.status==='active'?'نشط':'موقوف'}</span></td><td><button class="btn btn-secondary btn-sm" onclick="HR.openEmployeeModal(${e.id})">تعديل</button></td></tr>`).join(''):'<tr><td colspan="8" style="text-align:center;padding:25px;">لا يوجد موظفون مسجلون بعد.</td></tr>';
    this.populateEmployeeSelects();
  },
  populateEmployeeSelects() { ['hrAttendanceEmployee','hrLeaveEmployee','hrAdvanceEmployee'].forEach(id=>{const s=document.getElementById(id);if(s)s.innerHTML='<option value="">اختر الموظف...</option>'+this.employees.filter(e=>e.status==='active').map(e=>`<option value="${e.id}">${this.esc(e.employee_no)} — ${this.esc(e.full_name)}</option>`).join('');}); },
  async loadAttendance() { const date=document.getElementById('hrAttendanceDate')?.value||new Date().toISOString().slice(0,10); const json=await (await fetch('/api/hr/attendance?date='+encodeURIComponent(date))).json(); const tbody=document.getElementById('hrAttendanceTableBody'); if(!tbody||!json.success)return; tbody.innerHTML=(json.data||[]).length?json.data.map(a=>`<tr><td>${this.esc(a.employee_no)}</td><td>${this.esc(a.full_name)}</td><td>${this.statusLabel(a.status)}</td><td>${this.esc(a.check_in||'-')}</td><td>${this.esc(a.check_out||'-')}</td><td>${a.overtime_hours||0}</td><td>${this.esc(a.notes||'-')}</td></tr>`).join(''):'<tr><td colspan="7" style="text-align:center;padding:25px;">لا توجد سجلات لهذا التاريخ.</td></tr>'; },
  statusLabel(s){return ({present:'حاضر',absent:'غائب',late:'متأخر',leave:'إجازة'})[s]||s;},
  async loadLeaves() { const json=await (await fetch('/api/hr/leaves')).json(); const tbody=document.getElementById('hrLeavesTableBody');if(!tbody||!json.success)return;tbody.innerHTML=(json.data||[]).length?json.data.map(l=>`<tr><td>${this.esc(l.full_name)}</td><td>${this.esc(l.leave_type)}</td><td>${l.start_date} — ${l.end_date}</td><td>${l.days_count}</td><td>${this.esc(l.status==='pending'?'بانتظار الاعتماد':l.status==='approved'?'معتمدة':'مرفوضة')}</td><td>${l.status==='pending'?`<button class="btn btn-primary btn-sm" onclick="HR.setLeaveStatus(${l.id},'approved')">اعتماد</button>`:'-'}</td></tr>`).join(''):'<tr><td colspan="6" style="text-align:center;padding:25px;">لا توجد طلبات إجازة.</td></tr>'; },
  async loadAdvances() { const json=await (await fetch('/api/hr/advances')).json();const tbody=document.getElementById('hrAdvancesTableBody');if(!tbody||!json.success)return;tbody.innerHTML=(json.data||[]).length?json.data.map(a=>`<tr><td>${this.esc(a.full_name)}</td><td>${a.date}</td><td>${this.money(a.amount)}</td><td>${this.money(a.recovered_amount)}</td><td>${this.money(a.amount-a.recovered_amount)}</td><td>${this.money(a.installment_amount)}</td><td>${a.status==='active'?'نشطة':'مغلقة'}</td></tr>`).join(''):'<tr><td colspan="7" style="text-align:center;padding:25px;">لا توجد سلف مسجلة.</td></tr>'; },
  async loadPayroll() { const month=document.getElementById('hrPayrollMonth')?.value; const json=await (await fetch('/api/hr/payroll'+(month?'?month='+month:''))).json(); const tbody=document.getElementById('hrPayrollTableBody');if(!tbody||!json.success)return;tbody.innerHTML=(json.data||[]).length?json.data.map(p=>`<tr><td>${this.esc(p.full_name)}</td><td>${p.payroll_month}</td><td>${this.money(p.basic_salary)}</td><td>${this.money(p.overtime_amount)}</td><td>${this.money(p.deductions)}</td><td><b>${this.money(p.net_salary)}</b></td><td>${p.status==='paid'?'مصروف':'مسودة'}</td><td>${p.status!=='paid'?`<button class="btn btn-primary btn-sm" onclick="HR.payPayroll(${p.id})">اعتماد الصرف</button>`:'-'}</td></tr>`).join(''):'<tr><td colspan="8" style="text-align:center;padding:25px;">اختر الشهر ثم أنشئ مسير الرواتب.</td></tr>'; },
  openModal(id){document.getElementById(id)?.classList.add('active');}, closeModal(id){document.getElementById(id)?.classList.remove('active');},
  openEmployeeModal(id=null){const e=this.employees.find(x=>x.id===id);document.getElementById('hrEmployeeForm').reset();document.getElementById('hrEmployeeId').value=id||'';document.getElementById('hrEmployeeModalTitle').textContent=e?'تعديل بيانات الموظف':'إضافة موظف جديد'; if(e){['full_name','national_id','phone','job_title','department','basic_salary','bank_name','bank_account','notes'].forEach(k=>{const el=document.getElementById('hrEmp_'+k);if(el)el.value=e[k]||'';});document.getElementById('hrEmp_status').value=e.status;document.getElementById('hrEmp_employment_type').value=e.employment_type||'دوام كامل';document.getElementById('hrEmp_hire_date').value=e.hire_date||'';} this.openModal('hrEmployeeModal');},
  async submitEmployee(e){e.preventDefault();const id=document.getElementById('hrEmployeeId').value;const body={};['full_name','national_id','phone','job_title','department','employment_type','hire_date','basic_salary','status','bank_name','bank_account','notes'].forEach(k=>body[k]=document.getElementById('hrEmp_'+k).value);const res=await fetch('/api/hr/employees'+(id?'/'+id:''),{method:id?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const json=await res.json();if(json.success){this.closeModal('hrEmployeeModal');await this.load();}else alert(json.message);},
  async submitAttendance(e){e.preventDefault();const body={employee_id:document.getElementById('hrAttendanceEmployee').value,date:document.getElementById('hrAttendanceDate').value,status:document.getElementById('hrAttendanceStatus').value,check_in:document.getElementById('hrCheckIn').value,check_out:document.getElementById('hrCheckOut').value,overtime_hours:document.getElementById('hrOvertime').value,notes:document.getElementById('hrAttendanceNotes').value};const j=await (await fetch('/api/hr/attendance',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)})).json();if(j.success){e.target.reset();this.setToday();this.loadAttendance();this.loadDashboard();}else alert(j.message);},
  async submitLeave(e){e.preventDefault();const body={employee_id:document.getElementById('hrLeaveEmployee').value,leave_type:document.getElementById('hrLeaveType').value,start_date:document.getElementById('hrLeaveStart').value,end_date:document.getElementById('hrLeaveEnd').value,days_count:document.getElementById('hrLeaveDays').value,notes:document.getElementById('hrLeaveNotes')?.value||''};const j=await (await fetch('/api/hr/leaves',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)})).json();if(j.success){e.target.reset();this.loadLeaves();this.loadDashboard();}else alert(j.message);},
  async submitAdvance(e){e.preventDefault();const body={employee_id:document.getElementById('hrAdvanceEmployee').value,amount:document.getElementById('hrAdvanceAmount').value,date:document.getElementById('hrAdvanceDate').value,installment_amount:document.getElementById('hrAdvanceInstallment').value,notes:document.getElementById('hrAdvanceNotes')?.value||''};const j=await (await fetch('/api/hr/advances',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)})).json();if(j.success){e.target.reset();this.loadAdvances();this.loadDashboard();}else alert(j.message);},
  async setLeaveStatus(id,status){await fetch('/api/hr/leaves/'+id+'/status',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})});this.loadLeaves();this.loadDashboard();},
  async generatePayroll(){const month=document.getElementById('hrPayrollMonth').value;if(!month)return alert('اختر شهر المسير أولاً');const j=await (await fetch('/api/hr/payroll/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({payroll_month:month})})).json();alert(j.message);if(j.success){this.loadPayroll();this.loadDashboard();}},
  showPane(name, button){document.querySelectorAll('.hr-pane').forEach(p=>p.style.display='none');document.getElementById('hrPane_'+name).style.display='block';document.querySelectorAll('#hrView .report-tab-btn').forEach(b=>b.classList.remove('active'));button?.classList.add('active');},
  async payPayroll(id){const j=await (await fetch('/api/hr/payroll/'+id+'/pay',{method:'PUT',headers:{'Content-Type':'application/json'},body:'{}'})).json();if(j.success){this.loadPayroll();this.loadDashboard();}else alert(j.message);}
};
